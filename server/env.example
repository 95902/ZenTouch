# Configuration Render
NODE_ENV=production
PORT=10000

# Base de données (à configurer dans Render)
DATABASE_URL=your_database_url_here

# Session (à configurer dans Render)
SESSION_SECRET=your_session_secret_here
